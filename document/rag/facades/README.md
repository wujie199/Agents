# Facades — 对外 Port 层

实现 `core.ports.rag.RAGPort` 和 `core.ports.knowledge_base.KnowledgeBasePort`。

## 文件

| 文件 | 职责 |
|------|------|
| `rag.py` | `RAGPortAdapter` — RAG 检索 Port 实现 |
| `knowledge_base.py` | `KnowledgeBasePortAdapter` — 知识库建库 Port 实现 |
