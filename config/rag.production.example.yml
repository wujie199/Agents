# RAG 生产配置示例 — 增量覆盖 config/rag.yml
# 用法: export RAG_CONFIG=config/rag.production.example.yml

enable_graph_index: false

ingest:
  ocr_fast: false
  ocr_device: cpu

retrieval:
  use_mock_rerank_fallback: false
  enable_router: true
  auto_route: true
  rerank_min_score: 0.35

rewrite:
  enable_multi_query: false
