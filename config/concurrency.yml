# PolicyPort QPS/预算 + 聊天 API RPM 限流（max_qps 唯一来源）
default:
  max_qps_per_tenant: 100
  token_budget_per_user: 100000

chat:
  enabled: true
  tenant_requests_per_minute: 60
  user_requests_per_minute: 20
